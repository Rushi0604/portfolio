import { ReferenceHeroText } from "./ReferenceHeroText";

const ReferenceHeroTextDemo = () => {
  return (
    <div style={{ backgroundColor: "#0a0e17", minHeight: "100vh", width: "100%" }}>
      <ReferenceHeroText />
    </div>
  );
};

export default ReferenceHeroTextDemo;
// Default export for lazy loading if needed, or normal import
